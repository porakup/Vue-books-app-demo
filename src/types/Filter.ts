export default interface Filter {
        isEbook: string;
        isFree: string;
        priceRange: Array<number>;
        lang: string;
        printType: string;
        category: string;
        rating: string;
        sort: string;
    }